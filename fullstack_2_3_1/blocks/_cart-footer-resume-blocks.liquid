<div
  class="cart-footer-resume-blocks{% if block.settings.wrap_in_card %} card color-{{ block.settings.color_scheme }}{% endif %}"
  style="--margin-top: {{ block.settings.margin_top }}px; --margin-bottom: {{ block.settings.margin_bottom }}px;"
>
  {%- liquid
    assign locale_iso = request.locale.iso_code | default: localization.language.iso_code | default: 'fr' | downcase
    assign locale_base = locale_iso | split: '-' | first
    assign cart_totals_title = 'cart.estimated_total' | t
    assign cart_totals_sub = '(Excluding shipping)'
    if locale_base == 'fr'
      assign cart_totals_sub = '(Hors frais de livraison)'
    elsif locale_base == 'de'
      assign cart_totals_sub = '(Ohne Versandkosten)'
    endif

    assign real_cart_original_total_price = 0
    for item in cart.items
      assign compare_price = item.original_line_price
      if item.variant.compare_at_price > item.original_price
        assign compare_price = item.variant.compare_at_price | times: item.quantity
      endif
      assign real_cart_original_total_price = real_cart_original_total_price | plus: compare_price
    endfor

    assign cart_savings = 0
    if real_cart_original_total_price > cart.total_price
      assign cart_savings = real_cart_original_total_price | minus: cart.total_price
    elsif cart.original_total_price > cart.total_price
      assign cart_savings = cart.original_total_price | minus: cart.total_price
      assign real_cart_original_total_price = cart.original_total_price
    endif
  -%}

  {% content_for 'blocks' %}

  {%- if cart.cart_level_discount_applications.size > 0 -%}
    <div class="cart__discounts">
      {%- for discount in cart.cart_level_discount_applications -%}
        <div class="cart__discount">
          {% render 'icon', icon: 'shoppingmode' %}
          <span>{{ discount.title }} (−{{ discount.total_allocated_amount | money }})</span>
        </div>
      {%- endfor -%}
    </div>
  {%- endif -%}

  {%- if cart_savings > 0 -%}
    <div class="cart__reductions rs-cart__reductions">
      <p class="rs-cart__reductions-label">{{ 'cart.discount' | t }}</p>
      <div class="cart__reductions-value rs-cart__reductions-value">
        <span class="rs-cart__reductions-minus" aria-hidden="true">−&nbsp;</span>
        {% render 'price', price: cart_savings, sales_badge: 'none' %}
      </div>
    </div>
  {%- endif -%}

  <div class="cart__totals rs-cart__totals">
    <div class="rs-cart__totals-label">
      <p class="rs-cart__totals-title">{{ cart_totals_title }}</p>
      <span class="rs-cart__totals-sub">{{ cart_totals_sub }}</span>
    </div>
    <div class="cart__totals-value rs-cart__totals-value">
      {% render 'price', price: cart.total_price, sales_badge: 'none' %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:cart_footer_resume_blocks",
  "tag": null,
  "blocks": [
    {
      "type": "_cart-note"
    },
    {
      "type": "_cart-progress-bar"
    },
    {
      "type": "accordions"
    },
    {
      "type": "text"
    },
    {
      "type": "separator"
    },
    {
      "type": "_discount-code"
    },
    {
      "type": "cross-sell"
    },
    {
      "type": "slider"
    },
    {
      "type": "delivery-estimation"
    },
    {
      "type": "information-slider"
    }
  ],
  "settings": [
    {
      "type": "checkbox",
      "id": "wrap_in_card",
      "label": "t:wrap_in_card",
      "default": false
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:color_scheme",
      "default": "scheme-2",
      "visible_if": "{{ block.settings.wrap_in_card }}"
    },
    {
      "type": "header",
      "content": "t:margins"
    },
    {
      "type": "range",
      "id": "margin_top",
      "label": "t:margin_top",
      "default": 0,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "label": "t:margin_bottom",
      "default": 0,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    }
  ],
  "presets": [
    {
      "name": "t:cart_footer_resume_blocks"
    }
  ]
}
{% endschema %}
