# This file contains a list of files that will be excluded from Shopify CLI
# operations such as push, pull, dev, etc.
#
# Examples:
#
# Ignore a specific file:
#   templates/product.json
#
# Ignore templates with a wildcard pattern:
#   templates/*.json
#
# Ignore templates and sections with a regular expression:
#   /(templates|sections)/.*\.json/

# Keep app embed/app block config on Shopify side during pushes.
# Stops app embeds from being reset every deploy.
fullstack_2_3_1/config/settings_data.json

# Homepage hero images + section config — edit in theme editor, not via push.
fullstack_2_3_1/templates/index.json
